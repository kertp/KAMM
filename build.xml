<project name="KAMM" default="dist" basedir=".">
    <description>
        simple example build file
    </description>
  <!-- set global properties for this build -->
  <property name="src" location="src"/>
  <property name="build" location="build"/>
	<property name="build.classes" value="${build}/classes"/>
  <property name="dist"  location="dist"/>
  <property name="main"  location="Intro"/>
	
	<path id="compile.classpath">
			<fileset dir="lib">
				<include name="*.jar"/>
			</fileset>
		</path>

  <target name="init">
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="${build}"/>
  </target>

  <target name="build" depends="init"
        description="compile the source " >
    <!-- Compile the java code from ${src} into ${build} -->
    <javac srcdir="${src}" destdir="${build}"/>
  </target>

  <target name="dist" depends="build"
        description="generate the distribution" >
  	<propertyfile file="version.properties" comment="build number">
  		<entry key="build.revision.number" type="int" operation="+">
  	</propertyfile>
    <!-- Create the distribution directory -->
    <mkdir dir="${dist}/jar"/>

    <!-- Put everything in ${build} into the MyProject-${DSTAMP}.jar file -->
    <jar jarfile="${dist}/lib/kamm.jar" basedir="${build}"/>
  </target>
	
	<target name="jrun" description="Run jar file">

	</target>
	
 <!-- execute the program with the fully qualified name in ${build.dir} -->
  <target name="run" description="run the project" depends="build">
    <java dir="build\ee\ut\math\tvt\kamm" classname="Intro.class" fork="yes">
    </java>
  </target>

  <target name="clean"
        description="clean up" >
    <!-- Delete the ${build} and ${dist} directory trees -->
    <delete dir="${build}"/>
    <delete dir="${dist}"/>
  </target>

</project>